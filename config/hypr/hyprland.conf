# ============================================================
# ⚡ CYBER-TUNG HYPRLAND CONFIG v2.0
# Arch Linux + Hyprland + i7-6600U
#
# Thay đổi so với v1:
#   ✅ $scripts thống nhất → ~/CyberDotfiles/scripts
#   ✅ waybar có & để không block exec-once sau
#   ✅ cyberguard.sh → smartd.sh start
#   ✅ blur passes 3→2, size 10→6 (tiết kiệm GPU)
#   ✅ Super+M đổi thành Super+SHIFT+M (tránh exit nhầm)
#   ✅ Super+W → browser | Super+D → discord | Super+S → spotify
#   ✅ Thêm Super+SHIFT+P → purge khẩn cấp
# ============================================================

# ── BIẾN ────────────────────────────────────────────────────
$mainMod = SUPER
$scripts = ~/CyberDotfiles/scripts


# ── MÀN HÌNH ────────────────────────────────────────────────
monitor = , preferred, auto, 1


# ── TỰ ĐỘNG CHẠY ────────────────────────────────────────────
exec-once = nm-applet --indicator
exec-once = fcitx5 -d
exec-once = mako
exec-once = swww-daemon
exec-once = waybar &
exec-once = wal -R
exec-once = /usr/lib/polkit-kde-authentication-agent-1
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP

# Wallpaper — chờ swww-daemon khởi động
exec-once = sleep 0.5 && swww img "$(cat ~/.cache/wal/wal)" --transition-type simple

# Services
exec-once = tlp start
exec-once = gvfs-mount

# Smart daemon — quản lý CPU throttle tự động
exec-once = $scripts/smartd.sh start

# Monitor độc lập — theo dõi và cảnh báo
exec-once = $scripts/cyberguard_v2.sh

exec-once = noisetorch -i -t 85


# ── NHẬP LIỆU ───────────────────────────────────────────────
input {
    kb_layout    = us
    follow_mouse = 1
    sensitivity  = 0

    touchpad {
        natural_scroll = true
        tap-to-click   = true
    }
}


# ── GIAO DIỆN ───────────────────────────────────────────────
general {
    gaps_in  = 5
    gaps_out = 10
    border_size = 2

    col.active_border   = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)

    layout = dwindle
}


# ── TRANG TRÍ ───────────────────────────────────────────────
decoration {
    rounding = 12

    active_opacity   = 0.98
    inactive_opacity = 0.60

    blur {
        enabled = true
        size    = 6          # Giảm từ 10 → phù hợp i7-6600U
        passes  = 2          # Giảm từ 3 → tiết kiệm GPU đáng kể

        brightness = 1.2     # Giảm từ 1.5
        contrast   = 1.1
        vibrancy   = 0.4

        new_optimizations = true
        ignore_opacity    = true
    }

    shadow {
        enabled      = true
        range        = 20
        render_power = 3
        color        = rgba(0, 0, 0, 0.15)
    }
}


# ── MISC ────────────────────────────────────────────────────
misc {
    disable_hyprland_logo    = true
    disable_splash_rendering = true
    force_default_wallpaper  = 0
    mouse_move_enables_dpms  = true
    vfr                      = true
    vrr                      = 0
    animate_manual_resizes   = true
    mouse_move_focuses_monitor = true
    enable_swallow           = true
    swallow_regex            = ^(kitty)$
}


# ── ANIMATIONS ──────────────────────────────────────────────
animations {
    enabled = true

    bezier = shot, 0.2, 1, 0.2, 1

    animation = windows,    1, 5, shot, slide
    animation = windowsOut, 1, 5, shot, slide
    animation = fade,       1, 5, shot
    animation = fadeIn,     1, 4, shot
    animation = fadeOut,    1, 6, shot
    animation = workspaces, 1, 5, shot, slide
}


# ── LAYOUTS ─────────────────────────────────────────────────
dwindle {
    pseudotile       = true
    preserve_split   = true
}


# ════════════════════════════════════════════════════════════
# PHÍM TẮT
# ════════════════════════════════════════════════════════════

# ── 🚀 APPS ─────────────────────────────────────────────────
bind = $mainMod, Q,      exec, $scripts/apps.sh term        # Terminal
bind = $mainMod, Return, exec, $scripts/apps.sh term        # Terminal (alt)
bind = $mainMod, E,      exec, $scripts/apps.sh files       # File manager
bind = $mainMod, W,      exec, $scripts/apps.sh browser     # Browser
bind = $mainMod, D,      exec, $scripts/apps.sh discord     # Discord
bind = $mainMod, S,      exec, $scripts/apps.sh spotify     # Spotify (đổi từ M)
bind = $mainMod, SPACE,  exec, rofi -show drun              # App launcher


# ── 🛠️ HỆ THỐNG ─────────────────────────────────────────────
bind = $mainMod,       C,  killactive,
bind = $mainMod SHIFT, M,  exit,                            # Exit Hyprland (đổi từ M → SHIFT+M)
bind = $mainMod,       L,  exec, wlogout                    # Power menu
bind = $mainMod,       P,  exec, hyprlock                   # Lock screen
bind = $mainMod,       B,  exec, killall -SIGUSR1 waybar    # Toggle waybar
bind = $mainMod,       I,  exec, eww open --toggle control_center

# Purge khẩn cấp khi máy lag
bind = $mainMod SHIFT, P,  exec, $scripts/apps.sh purge

# Xem status smartd
bind = $mainMod SHIFT, D,  exec, $scripts/apps.sh status | kitty --hold -e cat


# ── 📸 SCREENSHOT ────────────────────────────────────────────
# Chụp vùng chọn
bind = $mainMod SHIFT, S, exec, grimblast --notify save area ~/Pictures/Screenshots/$(date +'%Y-%m-%d_%H-%M-%S').png
# Chụp toàn màn hình
bind = , Print,           exec, grimblast --notify save screen ~/Pictures/Screenshots/$(date +'%Y-%m-%d_%H-%M-%S').png


# ── 🖥️ CỬA SỔ ───────────────────────────────────────────────
bind = $mainMod, F,   fullscreen, 0
bind = $mainMod, V,   togglefloating,
bind = $mainMod, Tab, workspace, previous

# Focus
bind = $mainMod, left,  movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up,    movefocus, u
bind = $mainMod, down,  movefocus, d

# Resize submap
bind = $mainMod, R, submap, resize
submap = resize
binde = , right, resizeactive,  10 0
binde = , left,  resizeactive, -10 0
binde = , up,    resizeactive,  0 -10
binde = , down,  resizeactive,  0 10
bind  = , escape, submap, reset
submap = reset


# ── 🔢 WORKSPACES ────────────────────────────────────────────
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6

bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6


# ── 🔊 MEDIA KEYS ────────────────────────────────────────────
bindle = , XF86AudioRaiseVolume,  exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bindle = , XF86AudioLowerVolume,  exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindl  = , XF86AudioMute,         exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindl  = , XF86AudioMicMute,      exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindle = , XF86MonBrightnessUp,   exec, brightnessctl set 5%+
bindle = , XF86MonBrightnessDown, exec, brightnessctl set 5%-


# ── 🖱️ CHUỘT ────────────────────────────────────────────────
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow


# ── 🌈 TIỆN ÍCH ──────────────────────────────────────────────
bind = $mainMod, F1, exec, $scripts/keyhints.sh
bind = $mainMod, F2, exec, ~/scripts/wallpaper_engine.sh    # Giữ path cũ
